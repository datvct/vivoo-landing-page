import HeaderSettingsPage from "@/components/admin/settings/HeaderSettingsPage";

export const metadata = {
  title: "Header Menu Settings - Admin Panel",
};

export default function AdminMenuSettingsPage() {
  return <HeaderSettingsPage />;
}
